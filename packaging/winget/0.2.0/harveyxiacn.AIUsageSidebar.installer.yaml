# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
#
# The InstallerSha256 values are the digests of the published release assets
# for this PackageVersion. To recompute them, e.g. in PowerShell
#
#   (Get-FileHash .\AI.Usage.Sidebar_0.2.0_x64-setup.exe -Algorithm SHA256).Hash
#   (Get-FileHash .\AI.Usage.Sidebar_0.2.0_x64_en-US.msi -Algorithm SHA256).Hash
#
# `wingetcreate update harveyxiacn.AIUsageSidebar --urls <exe> <msi> --version <v>`
# does the same automatically for later releases.
PackageIdentifier: harveyxiacn.AIUsageSidebar
PackageVersion: 0.2.0
MinimumOSVersion: 10.0.17763.0
# The builds are unsigned, so SmartScreen warns on first run. Say so rather
# than pretending otherwise; see packaging/README.md.
ReleaseDate: 2026-09-21
Installers:
  # NSIS, per-user (tauri.conf.json → bundle.windows.nsis.installMode)
  - Architecture: x64
    InstallerType: nullsoft
    Scope: user
    InstallerUrl: https://github.com/harveyxiacn/AI-usage-monitor-sidebar/releases/download/v0.2.0/AI.Usage.Sidebar_0.2.0_x64-setup.exe
    InstallerSha256: 3D9BF2032665F0726281710F83FEDBA5C0E133AA558ABAB655D64814EFBB210E
    InstallerSwitches:
      Silent: /S
      SilentWithProgress: /S
    UpgradeBehavior: install
  # WiX/MSI, the alternative for centrally managed machines
  - Architecture: x64
    InstallerType: wix
    Scope: machine
    InstallerLocale: en-US
    InstallerUrl: https://github.com/harveyxiacn/AI-usage-monitor-sidebar/releases/download/v0.2.0/AI.Usage.Sidebar_0.2.0_x64_en-US.msi
    InstallerSha256: 3D29AF9A1B62F0D44935EBF08FA4EB606F87C81CC583FA729FB33A4F955C7D92
    UpgradeBehavior: install
ManifestType: installer
ManifestVersion: 1.6.0
